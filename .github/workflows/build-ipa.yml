name: iOS-ipa-build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:


permissions:
  contents: write

jobs:
  build-ios:
    name: iOS Build
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Ensure .env asset exists
        run: |
          if [ ! -f .env ]; then
            echo "GOOGLE_WEB_CLIENT_ID=32405567233-dq6bf22r6lglf4hdafj0ibrqqjd8i15a.apps.googleusercontent.com" > .env
          fi

      - run: flutter pub get

      - run: pod repo update
        working-directory: ios

      - run: flutter build ios --release --no-codesign

      - run: mkdir Payload
        working-directory: build/ios/iphoneos

      - run: mv Runner.app/ Payload
        working-directory: build/ios/iphoneos

      - name: Zip output
        run: zip -qq -r -9 FlutterIpaExport.ipa Payload
        working-directory: build/ios/iphoneos

      - name: Upload IPA to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ios-v1.0.${{ github.run_number }}
          name: iOS IPA v1.0.${{ github.run_number }}
          body: Automated unsigned iOS IPA build from workflow_dispatch.
          files: build/ios/iphoneos/FlutterIpaExport.ipa
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}